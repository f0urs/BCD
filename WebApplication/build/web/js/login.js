function usernameLoginClick() {
    var usernameSpan = document.getElementById("pswd");

    usernameSpan.className = ("un-span-top");
}

function usernameChangeLoginClick() {
    var usernameSpan = document.getElementById("pswd");

    usernameSpan.className = ("user-input-field");
}

function passwordLoginClick() {
    var passwordSpan = document.getElementById("pswd");

    passwordSpan.className = ("pw-span-top");
}

function passwordChangeLoginClick() {
    var passwordSpan = document.getElementById("pswd");

    passwordSpan.className = ("user-input-field");
}